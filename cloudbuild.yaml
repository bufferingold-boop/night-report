options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - "-t"
      - "asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/night-report-job:latest"
      - "."

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - "asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/night-report-job:latest"
